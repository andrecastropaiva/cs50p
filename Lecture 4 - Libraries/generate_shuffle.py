import random

cards = ["jack", "queen", "king"] # shuffling cards but could shuffle anything...
random.shuffle(cards)

for card in cards: # card is a name given just coz makes sense but could be named anything
    print(card)

